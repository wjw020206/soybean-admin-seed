# 应用的基本 URL，默认值为 "/"
# 如果使用子目录，则必须以 "/" 结尾，例如 "/admin/"，但不能是 "/admin"
VITE_BASE_URL=/

# 应用标签栏标题
VITE_APP_TITLE=SoybeanAdmin

# 应用描述
VITE_APP_DESC=SoybeanAdmin is a fresh and elegant admin template

# 图标名称的前缀
VITE_ICON_PREFIX=icon

# 本地 SVG 图标组件的前缀，必须包含 VITE_ICON_PREFIX
# 格式：{VITE_ICON_PREFIX}-{本地图标名称}
VITE_ICON_LOCAL_PREFIX=icon-local

# 权限路由模式：静态｜动态
VITE_AUTH_ROUTE_MODE=static

# 静态权限路由主页
VITE_ROUTE_HOME=home

# 默认菜单图标
VITE_MENU_ICON=mdi:menu

# 开发模式下是否启用 HTTP 代理（Y | N）
VITE_HTTP_PROXY=Y

# vue-router 模式: hash | history | memory
VITE_ROUTER_HISTORY_MODE=history

# 后端服务的成功状态码，当收到该状态码时，表示请求成功
VITE_SERVICE_SUCCESS_CODE=0000

# 后端服务的登出状态码，当收到该状态码时，用户将被登出并重定向到登录页面
VITE_SERVICE_LOGOUT_CODES=8888,8889

# 后端服务的模态登出状态码，当收到该状态码时，将通过显示模态框将用户登出
VITE_SERVICE_MODAL_LOGOUT_CODES=7777,7778

# 后端服务收到令牌过期状态码后，会刷新令牌并重新发送请求
VITE_SERVICE_EXPIRED_TOKEN_CODES=9999,9998,3333

# 当路由模式为静态时，定义的超级角色
VITE_STATIC_SUPER_ROLE=R_SUPER

# 是否生成 SourceMap
VITE_SOURCE_MAP=N

# 用于区分不同域的存储
VITE_STORAGE_PREFIX=SOY_

# 是否启用自动检测更新（Y | N）
VITE_AUTOMATICALLY_DETECT_UPDATE=Y

# 是否在终端中显示代理 URL 日志（Y | N）
VITE_PROXY_LOG=Y

# Vue DevTools 打开组件源码时使用的编辑器 例如: code / webstorm
# 顺便一提，此插件仅在开发模式下可用，在构建模式下不可用
VITE_DEVTOOLS_LAUNCH_EDITOR=code